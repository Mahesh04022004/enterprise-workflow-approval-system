spring:
  datasource:
    url: jdbc:mysql://localhost:3306/workflow_db
    username: root
    password: maheshvic123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
